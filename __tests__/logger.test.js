'use strict';

// jest.mock('socket.io-client');
// jest.mock('socket.io');
// require('../logger/logger.js');

describe('Logger loggs', () => {

    it('will console log a message from what it receives during a save event', () => {
      // io.socket.emit('examlpe', 'some messages');
      // setTimeout(() => {
      //   expect(message).toBe('some messages');
      //   done();
      // }, 50);
      expect(true).toBe(true);
    });

    it('will console error a message from what it receives during an err event', () => {
      expect(true).toBe(true);
    });
});